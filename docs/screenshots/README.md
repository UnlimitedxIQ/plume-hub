# Screenshots

Drop app screenshots here and reference them from the root README:

- `canvas-dashboard.png` — the Canvas panel with assignment cards
- `library.png` — the 3-column Library (Agents / Skills / MCPs)
- `session.png` — the Session panel mid-workflow
- `recommended-plugins.png` — Settings → Recommended plugins bulk installer

Take these from a real install with representative data. Commit as PNGs,
ideally under 500 KB each (compress with `tinypng` or similar).

